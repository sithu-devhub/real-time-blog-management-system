// Create/Edit Post Page - Auto-grow textarea script

// function autoGrow(element) {
//     const savedScrollTop = document.documentElement.scrollTop || document.body.scrollTop;
  
//     function autoGrow(element) {
//         element.style.height = 'auto'; // Reset height
//         element.style.height = element.scrollHeight + 'px'; // Set new height based on scroll height
//       }
  
//     // Restore scroll position asynchronously
//     requestAnimationFrame(() => {
//       document.documentElement.scrollTop = savedScrollTop;
//       document.body.scrollTop = savedScrollTop;
//     });
//   }

